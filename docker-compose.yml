version: '3.8'

services:
  server:
    image: react-video-stream
    container_name: server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    volumes:
      - ./app/src:/src
    networks:
      - mynetwork

networks:
  mynetwork: